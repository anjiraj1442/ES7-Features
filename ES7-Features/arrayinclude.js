let arr = [1, 2, 3, 4];
if(arr.includes(4)) { 
      console.log('Array contains value');
     
     } else{
          console.log("not contaim any value")
     }

     //based on index value find the number
     let arr = [1, 2, 3, 4];
if(arr.includes(2,3)) { 
      console.log('Array contains value');
     
     } else{
          console.log("not contaim any value")
     }